# Measured in the environment that verified this release (2026-09-24); Python 3.12.10.
# The only version pinned by a record inside the shipped results is scikit-learn
# (1.7.2, recorded by the learner artifacts); everything below is this machine's
# environment, quoted so a reviewer can match it rather than claim equivalence.
numpy==2.2.6
scikit-learn==1.7.2
matplotlib==3.11.1
PyMuPDF==1.28.2
# tier 3 additionally needs torch==2.9.1+cu126 and BasicTS 0.5.8 (directory name BasicTS058;
# our local copy is not a git checkout, so no commit hash is recorded for it).
